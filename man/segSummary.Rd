% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segSummary.R
\name{segSummary}
\alias{segSummary}
\alias{print.segSummary}
\alias{as.data.frame.segSummary}
\title{Segment summary object}
\usage{
\method{print}{segSummary}(x, digits = max(3L, getOption("digits") - 3L), ...)

\method{as.data.frame}{segSummary}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{segSummary} object.}

\item{digits}{Integer. Significant digits for printing. Default: \code{max(3L, getOption("digits") - 3L)}.}

\item{...}{Ignored.}

\item{row.names, optional}{Ignored; present for compatibility with the generic.}
}
\value{
\code{print()} invisibly returns \code{x}. \code{as.data.frame()} returns a \code{data.frame} with one row per segment:
\code{segment}, \code{start}, \code{end}, \code{n}, \code{cost}, then flattened parameters (\code{mean.X1}, \verb{coef.(Intercept).X1}, ...).
}
\description{
A list with one element per segment, produced by \verb{$summary()} of \code{PELT}, \code{binSeg}, \code{Window}
and \code{costFactory}. Each element is a named list with \code{start} (exclusive, 0-based), \code{end} (inclusive),
\code{n} (number of observations), \code{cost}, followed by the fitted parameters of the cost function:
\itemize{
\item \code{"L1"}: \code{median} (coordinate-wise median), \code{scale} (mean absolute deviation from the median).
\item \code{"L2"}: \code{mean}, \code{var} (per-coordinate residual variance, divisor \code{n}).
\item \code{"SIGMA"}: \code{mean}, \code{cov} (empirical covariance, divisor \code{n}, without \code{epsilon}).
\item \code{"VAR"}: \code{coef} (rows \code{(Intercept)}, \code{X1.lag1}, ..., one column per response), \code{var}.
\item \code{"LinearL2"}: \code{coef} (rows \code{(Intercept)} and covariates, one column per response), \code{var}.
}

Dispersion entries use the maximum-likelihood divisor so that, e.g., \code{sum(var) * n} equals \code{cost} for \code{"L2"}.
Coefficients are \code{NA} where the segment is too short (\code{n < J}) or the system is rank deficient (there the \verb{C++}
cost uses a pseudo-inverse). The \code{costFunc} configuration is stored as \code{attr(x, "costFunc")}.
}
\examples{
set.seed(1)
tsMat = as.matrix(c(rnorm(100, 0), rnorm(100, 5)))
obj = binSeg$new(); obj$fit(tsMat); obj$predict(pen = 50)
s = obj$summary()
s
as.data.frame(s)
s[[2]]$mean
}
