% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/costFactoryR6.R
\docType{class}
\name{costFactory}
\alias{costFactory}
\title{\code{costFactory} class}
\description{
An \code{R6} class for fast segment-cost evaluation and parameter estimation, without a detection algorithm.
}
\details{
\code{costFactory} builds the \verb{C++} cost module selected by a \code{costFunc} object once, at initialisation, and keeps it,
so every query reuses its precomputations, as \code{PELT}, \code{binSeg} and \code{Window} do. \verb{$eval()} and \verb{$get_params()} call
the module directly: segments are \verb{(a, b]} with 0-based \code{a}, and all checks are done in \verb{C++}.

\verb{$get_params()} returns:
\itemize{
\item \code{"L1"}: \code{median}.
\item \code{"L2"}: \code{mean}.
\item \code{"SIGMA"}: \code{mean} and \code{cov} (plus \code{epsilon} on the diagonal if \code{addSmallDiag = TRUE}).
\item \code{"VAR"}, \code{"LinearL2"} and \code{"LinearL1"}: \code{coef}, intercept first.
\item \code{"LinearSIGMA"}: \code{coef} and \code{cov}.
\item \code{"Custom"}: \code{params}, the output of \code{paramFun}; an empty list if \code{paramFun} is \code{NULL}.
}
}
\section{Methods}{

\describe{
\item{\code{$new()}}{Initialises a \code{costFactory} object.}
\item{\code{$eval()}}{Evaluates the cost of a segment.}
\item{\code{$get_params()}}{Returns the parameter estimates of a segment.}
\item{\code{$clone()}}{Clones the \code{R6} object.}
}
}

\examples{
set.seed(1)
tsMat = cbind(c(rnorm(100, 0), rnorm(100, 5, 5)))
cf = costFactory$new(costFunc$new("L2"), tsMat)
cf$eval(0, 100)
cf$get_params(0, 100)
}
\author{
Minh Long Nguyen \email{edelweiss611428@gmail.com}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-costFactory-new}{\code{costFactory$new()}}
\item \href{#method-costFactory-eval}{\code{costFactory$eval()}}
\item \href{#method-costFactory-get_params}{\code{costFactory$get_params()}}
\item \href{#method-costFactory-clone}{\code{costFactory$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-costFactory-new"></a>}}
\if{latex}{\out{\hypertarget{method-costFactory-new}{}}}
\subsection{Method \code{new()}}{
Initialises a \code{costFactory} object and builds the \verb{C++} cost module.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{costFactory$new(costFunc, tsMat, covariates = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{costFunc}}{A \code{R6} object of class \code{costFunc}. Default: \code{costFunc$new("L2")}.}

\item{\code{tsMat}}{Numeric matrix. Time series of size \eqn{n \times p}.}

\item{\code{covariates}}{Numeric matrix with \code{n} rows, used by \code{"LinearL2"}, \code{"LinearSIGMA"} and \code{"LinearL1"}. If \code{NULL},
the model is force-fitted with only an intercept. Default: \code{NULL}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
Invisibly returns \code{NULL}.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-costFactory-eval"></a>}}
\if{latex}{\out{\hypertarget{method-costFactory-eval}{}}}
\subsection{Method \code{eval()}}{
Evaluates the cost of the segment (a, b] with the module's \code{eval()}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{costFactory$eval(a, b)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{a}}{Integer. Start index (exclusive, 0-based).}

\item{\code{b}}{Integer. End index (inclusive).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
The segment cost.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-costFactory-get_params"></a>}}
\if{latex}{\out{\hypertarget{method-costFactory-get_params}{}}}
\subsection{Method \code{get_params()}}{
Returns the parameter estimates of the segment (a, b] with the module's \code{get_params()}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{costFactory$get_params(a, b)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{a}}{Integer. Start index (exclusive, 0-based).}

\item{\code{b}}{Integer. End index (inclusive).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A named list; see Details.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-costFactory-clone"></a>}}
\if{latex}{\out{\hypertarget{method-costFactory-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{costFactory$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
